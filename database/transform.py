def transform_data(data):
    # Transformationslogik hier implementieren
    return data

if __name__ == "__main__":
    sample = {"key": "value"}
    transformed = transform_data(sample)
    print("Transformierte Daten:", transformed)
